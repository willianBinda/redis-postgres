# 1 - Criar um arquivo .env e adicionar as seguintes variáveis

POSTGRES_HOST=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

REDIS_HOST=
REDIS_PORT=
REDIS_DB=1
REDIS_PASSWORD=

# 2 - Utilizado virtualenv, necessário rodar o comando `pip install -r requirements.txt` no terminal para instalar as bibliotecas

# 2 - Dentro da pasta db existe a conexão com redis e postgres

# 3 - Na main.py existe o script para consultar no postgres e inserir no redis
